{
  "name": "@clide/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute clide-db --local --file=./migrations/0001_initial.sql && wrangler d1 execute clide-db --local --file=./migrations/0002_add_composite_indexes.sql && wrangler d1 execute clide-db --local --file=./migrations/0003_password_auth_and_seed.sql && wrangler d1 execute clide-db --local --file=./migrations/0004_domains_and_short_links.sql",
    "db:migrate:remote": "wrangler d1 execute clide-db --remote --file=./migrations/0001_initial.sql && wrangler d1 execute clide-db --remote --file=./migrations/0002_add_composite_indexes.sql && wrangler d1 execute clide-db --remote --file=./migrations/0003_password_auth_and_seed.sql && wrangler d1 execute clide-db --remote --file=./migrations/0004_domains_and_short_links.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "wrangler": "^3.91.0",
    "typescript": "^5.6.0"
  }
}
